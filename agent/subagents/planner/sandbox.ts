/**
 * A notebook, not a machine: a persistent /workspace with no container and no
 * VM. See `lib/notebook-sandbox.ts` for why this is pinned rather than left to
 * the framework default.
 */
export { default } from "../../lib/notebook-sandbox";
