/**
 * See `lib/notebook-write-file.ts`: the refusal becomes a pointer to
 * `append_note`, which is what the agent almost always wanted.
 */
export { default } from "../../../lib/notebook-write-file";
